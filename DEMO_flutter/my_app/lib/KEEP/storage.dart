
import 'package:flutter/material.dart';

class storage extends StatelessWidget{
  const storage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Center(child: Text("Lưu trữ")),
      ),
      body: Center(child: const Text("New Screen")),
    );
  }

}